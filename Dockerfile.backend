FROM node:20-alpine

WORKDIR /app

ENV NODE_ENV=production

COPY backend/package*.json ./
RUN npm ci --omit=dev || npm install --omit=dev

COPY backend/ ./

RUN mkdir -p uploads

EXPOSE 5000

CMD ["npm", "start"]
